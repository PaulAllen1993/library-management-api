{
  "name": "library-api",
  "version": "0.4.3",
  "description": "Library API",
  "main": "index.js",
  "scripts": {
    "release": "release-it"
  },
  "release-it": {
    "preset": "angular",
    "plugins": {
      "@release-it/conventional-changelog": {
        "infile": "CHANGELOG.md",
        "preset": "angular",
        "writerOpts": {
          "mainTemplate": "{{#if this.isPatch}}\n## {{this.version}} ({{this.date}})\n{{else}}\n# {{this.version}} ({{this.date}})\n{{/if}}\n\n{{#if this.compareLink}}[**Full Changelog**]({{this.compareLink}}){{/if}}\n\n{{#each this.commitGroups}}### {{this.title}}\n\n{{#each this.commits}}* {{#if this.scope}}**{{this.scope}}:** {{/if}}{{this.subject}} ([{{this.shortHash}}]({{this.commit.url}})){{#if this.files}} - Files: `{{#each this.files}}{{this}}{{#unless @last}}`, `{{/unless}}{{/each}}`{{/if}}\n{{/each}}\n{{/each}}\n"
        }
      }
    },
    "git": {
      "commitMessage": "chore: release v${version}",
      "tagName": "v${version}",
      "requireCleanWorkingDir": false,
      "push": true,
      "pushArgs": "--follow-tags"
    },
    "npm": {
      "publish": false
    },
    "github": {
      "release": true,
      "releaseName": "Release v${version}"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/PaulAllen1993/library-management-api.git"
  },
  "author": "Paul Allen",
  "license": "ISC",
  "devDependencies": {
    "@release-it/conventional-changelog": "^10.0.5",
    "release-it": "^19.2.4"
  }
}
